langgraph>=0.3.0
langchain-core>=0.3.0
langchain-groq>=0.2.0
chromadb>=0.5.0
fastapi>=0.115.0
uvicorn>=0.30.0
python-telegram-bot>=21.0
streamlit>=1.40.0
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic>=2.9.0
pytest>=8.0.0

# UI extras (Streamlit dashboard polish — animated pipeline, radial gauges)
plotly>=5.20.0
streamlit-extras>=0.4.0
streamlit-lottie>=0.0.5

# Hybrid retrieval (BM25 + dense fusion). Pure-Python, no native deps.
rank_bm25>=0.2.2

# OPTIONAL — only needed if you want to re-run the embedding-comparison
# arm of eval/sweep.py (sentence-transformers / BGE-small). Pulls torch
# (~2 GB), so kept commented for the production install path.
# sentence-transformers>=3.0.0
